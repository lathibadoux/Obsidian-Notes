# Navigate to your project
cd ~/Documents/ai_bootcamp/ai-engineering-bootcamp-app/ai-application-october-2025

# Activate virtual environment
*Temporarily tells shell to use the Python in your .venv, not the global one*
source venv/Scripts/activate    # or .\venv\Scripts\Activate.ps1

# Start the FastAPI server
uvicorn main:app --reload --port 8000
*Open new terminal to keep uvicorn running*
# Deactivate when done
deactivate
